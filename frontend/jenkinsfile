pipeline {
    agent any

    tools {
        nodejs 'Node22'
    }

    stages {
        stage('Build') {
            steps {
                sh 'node -v'
                sh 'npm -v'
            }
        }
    }
}