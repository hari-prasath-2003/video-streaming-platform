{
	"scripts": {
		"dev": "concurrently -n services,web -c cyan,magenta \"pnpm run dev:services\" \"pnpm run dev:web\"",
		"dev:services": "pnpm -r --parallel run dev",
		"dev:web": "pnpm --dir frontend run dev",
		"build": "pnpm -r run build"
	},
	"devDependencies": {
		"@types/node": "^26.1.1",
		"concurrently": "^10.0.4",
		"tsx": "^4.23.1",
		"typescript": "^7.0.2"
	}
}
